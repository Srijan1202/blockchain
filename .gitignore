node_modules/
dist/

# Secrets. .env.example is committed; .env never is.
.env

# Measurement artifacts. The database and exports are regenerated by the
# harness, not tracked. data/.gitkeep keeps the directory in the tree.
data/*.sqlite*
data/*.csv
data/export_manifest.json

# Python build output
__pycache__/
*.pyc

# Scan logs from the mainnet indexer
data/*.log
